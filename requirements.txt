# Runtime dependencies, pinned to the versions the reported results were
# produced with.  Loosen deliberately, then re-run `make reproduce`.
keybert==0.9.0
sentence-transformers==5.1.2
transformers==4.57.6
torch==2.8.0
scikit-learn==1.6.1
scipy==1.13.1
pandas==2.3.3
numpy==2.0.2
nltk==3.9.2
spacy==3.5.4
langdetect==1.0.9
pyyaml==6.0.3
tqdm==4.67.3

# Optional: LLM keyword validation and the LLM zero-shot baseline
openai==2.34.0

# Optional: only for the translation ablation
# (`run.py --extra-ablations`, Helsinki-NLP/opus-mt-de-en)
sentencepiece==0.2.2

# Development
pytest==8.4.2
flake8==7.3.0
